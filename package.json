{
  "name": "plugin-fizzl",
  "version": "0.4.0",
  "description": "ElizaOS plugin for Fizzl's x402 agent services: Ichimoku Cloud trading signals, PlainText wallet-approval risk checks, x402 Doctor pre-payment checks and endpoint diagnosis, and presign-guard checks before signing. Pays per call in USDC on Solana or Base.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Fizzl13/plugin-fizzl"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test"
  },
  "peerDependencies": {
    "@elizaos/core": "^1.0.0"
  },
  "dependencies": {
    "@solana/kit": "^8.3.0",
    "@x402/core": "^2.27.0",
    "@x402/evm": "^2.27.0",
    "@x402/fetch": "^2.27.0",
    "@x402/svm": "^2.27.0",
    "viem": "^2.37.0"
  },
  "devDependencies": {
    "@elizaos/core": "1.7.2",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "agentConfig": {
    "pluginType": "elizaos:plugin:1.0.0",
    "pluginParameters": {
      "SVM_PRIVATE_KEY": {
        "type": "string",
        "description": "Solana secret key (base58) that pays Fizzl services in USDC on Solana (all except presign-guard, which is Base only)",
        "required": false,
        "sensitive": true
      },
      "EVM_PRIVATE_KEY": {
        "type": "string",
        "description": "EVM private key (0x…) that pays in USDC on Base; needed for presign-guard, otherwise optional when SVM_PRIVATE_KEY is set",
        "required": false,
        "sensitive": true
      },
      "FIZZL_MAX_PAYMENT_USD": {
        "type": "string",
        "description": "Refuse any single payment above this amount in USD",
        "required": false,
        "default": "0.25"
      },
      "ICHIMOKU_SIGNAL_URL": {
        "type": "string",
        "description": "Ichimoku Signal base URL",
        "required": false,
        "default": "https://ichimoku-signal.onrender.com"
      },
      "PLAINTEXT_URL": {
        "type": "string",
        "description": "PlainText base URL",
        "required": false,
        "default": "https://smartcontractexplainer.onrender.com"
      },
      "PRESIGN_GUARD_URL": {
        "type": "string",
        "description": "presign-guard base URL",
        "required": false,
        "default": "https://presign-guard.onrender.com"
      },
      "SOLANA_RPC_URL": {
        "type": "string",
        "description": "Solana RPC used to build payments (default: public mainnet RPC)",
        "required": false
      }
    }
  },
  "keywords": [
    "elizaos",
    "elizaos-plugin",
    "x402",
    "solana",
    "base",
    "trading",
    "ichimoku",
    "wallet-security",
    "approvals",
    "diagnostics",
    "safety",
    "presign",
    "eip-712"
  ],
  "license": "MIT"
}
